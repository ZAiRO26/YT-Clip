{
  "name": "clipforge-monorepo",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @clipforge/web dev",
    "build:web": "pnpm --filter @clipforge/web build",
    "lint": "pnpm --filter @clipforge/web lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
